<Window x:Class="HSMS.Desktop.AnalyticsDrilldownWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Analytics — Drill-down"
        Height="720" Width="1100"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Name="BreadcrumbText" FontSize="16" FontWeight="SemiBold" />
        <TextBlock Grid.Row="1" Name="RangeText" Margin="0,6,0,10" Foreground="#6B7280" />

        <DataGrid Grid.Row="2" Name="RowsGrid"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal"
                  SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Cycle No" Binding="{Binding CycleNo}" Width="110"/>
                <DataGridTextColumn Header="Sterilizer" Binding="{Binding SterilizerNo}" Width="110"/>
                <DataGridTextColumn Header="Status" Binding="{Binding CycleStatus}" Width="100"/>
                <DataGridTextColumn Header="Operator" Binding="{Binding OperatorName}" Width="160"/>
                <DataGridTextColumn Header="Registered (UTC)" Binding="{Binding RegisteredAtUtc}" Width="150"/>
                <DataGridTextColumn Header="Cycle Date/Time (UTC)" Binding="{Binding CycleDateTimeUtc}" Width="170"/>
                <DataGridTextColumn Header="Pcs" Binding="{Binding TotalPcs}" Width="70"/>
                <DataGridTextColumn Header="Qty" Binding="{Binding TotalQty}" Width="70"/>
            </DataGrid.Columns>
        </DataGrid>

        <DockPanel Grid.Row="3" Margin="0,12,0,0" LastChildFill="False">
            <TextBlock Name="PagerText" DockPanel.Dock="Left" VerticalAlignment="Center" Foreground="#6B7280"/>
            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                <Button Name="PrevButton" Content="Prev" MinWidth="80" Margin="0,0,8,0"/>
                <Button Name="NextButton" Content="Next" MinWidth="80" Margin="0,0,8,0"/>
                <Button Name="CloseButton" Content="Close" MinWidth="80"/>
            </StackPanel>
        </DockPanel>
    </Grid>
</Window>

