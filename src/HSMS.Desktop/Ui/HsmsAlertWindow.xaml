<Window x:Class="HSMS.Desktop.Ui.HsmsAlertWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="HSMS"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        ResizeMode="NoResize"
        Background="Transparent"
        AllowsTransparency="True"
        ShowInTaskbar="False"
        Topmost="True">
    <Border Background="{StaticResource BrushCard}"
            BorderBrush="{StaticResource BrushBorder}"
            BorderThickness="1"
            CornerRadius="14"
            Padding="18">
        <Grid Width="420">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="14"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="18"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="14"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Width="44" Height="44" CornerRadius="12" Background="{Binding AccentBackground}">
                <TextBlock Text="{Binding IconGlyph}"
                           Foreground="{Binding AccentForeground}"
                           FontSize="22"
                           FontWeight="SemiBold"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"/>
            </Border>

            <StackPanel Grid.Column="2">
                <TextBlock Text="{Binding TitleText}"
                           FontSize="16"
                           FontWeight="SemiBold"
                           Foreground="#212529"
                           TextWrapping="Wrap"/>
                <TextBlock Text="{Binding MessageText}"
                           Margin="0,6,0,0"
                           FontSize="13"
                           Foreground="{StaticResource BrushMuted}"
                           TextWrapping="Wrap"
                           LineHeight="18"/>
            </StackPanel>

            <StackPanel Grid.Row="4" Grid.ColumnSpan="3" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="OK" MinWidth="110" Style="{StaticResource PrimaryButton}" Click="OkButton_OnClick"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>

