<Window x:Class="HSMS.Desktop.Views.TextPromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="HSMS"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{StaticResource BrushSurface}"
        FontFamily="Segoe UI">
    <Border Background="{StaticResource BrushCard}" BorderBrush="{StaticResource BrushBorder}" BorderThickness="1" CornerRadius="12" Padding="14" Margin="12">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="8"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="12"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Name="PromptText" Grid.Row="0" FontSize="12" Foreground="#111827" TextWrapping="Wrap"/>

            <TextBox Name="InputBox" Grid.Row="2" MinWidth="360"/>

            <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cancel" Style="{StaticResource SecondaryToolbarButton}" MinWidth="95" Margin="0,0,10,0" Click="Cancel_OnClick"/>
                <Button Content="OK" Style="{StaticResource PrimaryButton}" MinWidth="95" Click="Ok_OnClick"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>

